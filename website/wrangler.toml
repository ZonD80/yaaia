# Cloudflare Pages — static assets from `nuxt generate` (`.output/public`)
# Custom domain: add yaaia.online in Cloudflare Dashboard → Pages → Custom domains
name = "yaaia-website"
compatibility_date = "2024-11-01"
pages_build_output_dir = ".output/public"
